# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
# Usually node_modules should be ignored. Add exceptions if you need to commit specific packages.
/node_modules

# Build artifacts
# Common build output directories
/build
/dist
/out
/.next # If using Next.js
/.svelte-kit # If using SvelteKit (less likely for React, but good general practice)

# TypeScript specific build output
*.tsbuildinfo
tsconfig.tsbuildinfo # Specific file often generated

# Logs
# Log files from npm, yarn, pnpm, or other tools
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Environment variables
# Never commit secrets! Use .env.example or similar for templates.
.env
.env.local
.env.development.local
.env.production.local
.env.test.local
.env.*.local

# dotenv environment variables file
.env.development
.env.production
.env.test

# Editor directories and files
# VS Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# JetBrains IDEs (WebStorm, IntelliJ, etc.)
.idea/

# Sublime Text
*.sublime-project
*.sublime-workspace

# Other
.project
.settings/
.classpath

# Testing
# Output from test runners and coverage reports
/coverage
.nyc_output

# Operating system files
.DS_Store
._*
Thumbs.db

# Miscellaneous
# Temporary files, cache, etc.
*.tmp
*.swp
*.bak
*.orig

# Optional: Yarn v2+ / Berry cache and install state files
# Only ignore these if you aren't using Zero-Installs
# .yarn/cache
# .yarn/unplugged
# .yarn/build-state.yml
# .yarn/install-state.gz
# .pnp.*
